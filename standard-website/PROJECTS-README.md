# Projects README

This file lists all project tracks currently available on the Development Standards website.

## WeWeb + Supabase

- `01 — Team Member Directory` — `~8 Hours`
  Table schema, RLS, Storage for avatars, WeWeb list/detail/form pages, search, filters, and loading/empty/error states.
- `02 — Task Board` — `~8 Hours`
  Multi-table schema, RPC functions, RLS, WeWeb task list with filters and bulk actions, add/edit modal, and delete confirmation.

## WeWeb + Xano

- `01 — Contact Book` — `~8 Hours`
  Xano CRUD endpoints, reusable permission function, WeWeb list and form pages, search, filters, pagination, and all states.
- `02 — Leave Request System` — `~8 Hours`
  Xano multi-step status workflow, WeWeb multi-step form, approval flow, role-based views, and date validation.

## React + Django

- `01 — Announcement Board` — `~8 Hours`
  Django model, DRF API, service layer, and React pages/forms with permissions plus loading/empty/error states.
- `02 — Expense Tracker` — `~8 Hours`
  Django model, file upload, approval workflow, DRF backend, and React list, form, and admin-facing views.

## React + Supabase

- `01 — Workspace Invite System` — `~8 Hours`
  Supabase tables, RLS, invite email Edge Function, accept-invite RPC, React invite management, and token-based invite acceptance.
- `02 — Document Library` — `~8 Hours`
  Supabase Storage, RLS on `storage.objects`, create-folder RPC, React file upload, signed URL download, and folder navigation.

## Django + Templates

- `01 — Leave Request System` — `~8 Hours`
  Django models, forms, services, views, and templates with 3-folder structure, `base.html`, data attributes, and status workflow.
- `02 — Internal Knowledge Base` — `~8 Hours`
  Django models, forms, services, views, and templates with search, categories, permissions, rich content, and slug routing.

## Total

- `10 projects`
- `~80 hours` total if completed end-to-end
