# Setup

Open `standard-website/index.html` in a browser or serve the folder with any static server.

## Optional Local Server

Examples:

```bash
python3 -m http.server
```

or

```bash
npx serve .
```
